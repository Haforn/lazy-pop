<h2>Create Competency</h2>

<%= form_for(:competency, :url => {:action => 'create', :user_id => current_user.id}) do |f| %> 

	<%= render(:partial => "form", :locals => {:f => f}) %>

	<br>

	<div class="form-buttons">
		<%= submit_tag("Create Competency", :class => "btn new bigger") %>
	</div>

<% end %>