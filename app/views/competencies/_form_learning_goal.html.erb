<%= error_messages_for(@learning_goal) %>

<li>
	<%= l.label(:goal, "Learnig Goal") %>
	<%= l.text_area(:goal)  %>
</li>

<li>
	<%= l.check_box(:goal_made) %>
	<%= l.label(:goal_made) %>
</li>

<%= l.hidden_field(:competency_id, :value => @competency.id) %>